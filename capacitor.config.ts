import type { CapacitorConfig } from '@capacitor/cli'

const config: CapacitorConfig = {
  appId: 'com.jhunar.levelup',
  appName: 'Level Up',
  webDir: 'dist',
  backgroundColor: '#0b0a12',
  android: {
    backgroundColor: '#0b0a12',
  },
}

export default config
